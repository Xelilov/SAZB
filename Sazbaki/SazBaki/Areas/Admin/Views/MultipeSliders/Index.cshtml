@model IEnumerable<SazBaki.Models.MultipeSlider>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@*<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-secondary" })
</p>*@
<table class="table">
    <tr>
        <th>
            <h4>Logo</h4>
        </th>
        <th>
            <h4>Image</h4>
        </th>
        <th style="width:200px;">
            <h4>Text in Image</h4>
        </th>
        <th>
            <h4>Language</h4>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <div style="width:100px; height:50px;">
                <img src="~/Uploads/@item.mp_logo_img" style="width:100%;" />
            </div>
        </td>
        <td>
            <div style="width:200px; height:100px;">
                <img src="~/Uploads/@item.mp_img" style="width:100%;" />
            </div>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mp_text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Language.language1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }, new { @class = "btn btn-success" })
            @Html.ActionLink("Details", "Details", new { id=item.Id }, new { @class = "btn btn-primary" })
            @*@Html.ActionLink("Delete", "Delete", new { id=item.Id }, new { @class = "btn btn-danger" })*@
        </td>
    </tr>
}

</table>
